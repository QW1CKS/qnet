# Example env for running the edge gateway locally
# Generate a self-signed cert for testing (do not use in production):
#   openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 1 -nodes -subj "/CN=localhost"

BIND=0.0.0.0:4443
HTX_TLS_CERT=cert.pem
HTX_TLS_KEY=key.pem
